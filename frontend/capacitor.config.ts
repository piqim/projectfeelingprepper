import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.piqim.feelingprepper',
  appName: 'FeelingPrepper',
  webDir: 'dist'
};

export default config;
